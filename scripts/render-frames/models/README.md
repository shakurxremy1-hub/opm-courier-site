# Truck model credit

`delivery-truck.glb` — low-poly delivery truck by **Quaternius**, via
[poly.pizza](https://poly.pizza/m/cXw6oiFtZ8), licensed **CC0 (public domain)**.
No attribution required, but credited here anyway.

Used as a wireframe-edge source (see `../scene.html`) — the shaded surfaces are
discarded and only the mesh edges are rendered, in the site's glowing
cyan/gold line style.
